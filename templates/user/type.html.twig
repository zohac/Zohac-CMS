{% extends 'base.html.twig' %}

{% block title %}Type!{% endblock %}

{% block body %}
    <div class="form-wrapper">
        {{ form_start(form, {'action': path('users.create'), 'method': 'POST'}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
