{{ entity.shortName }}:
The {{ entity.shortNameToLower }} was not found.:
Are you sure you want to delete this {{ entity.shortNameToLower }} ?:
        {{ entity.shortName }} successfully created.:
        {{ entity.shortName }} successfully deleted.:
        {{ entity.shortName }} successfully updated.:
        {% for property in entity.properties %}
        {% if property.name not in ['id', 'uuid'] %}
        {{ property.name }}:
        {% endif %}
        {% endfor %}
